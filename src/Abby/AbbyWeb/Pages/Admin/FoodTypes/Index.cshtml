@page
@model AbbyWeb.Pages.Admin.FoodTypes.IndexModel

<div class="container p-3">
	<div class="row pt-4">
		<div class="col-6">
			<h2 class="text-primary">Food Type List</h2>
		</div>
		<div class="col-6 text-end">
			<a asp-page="Create" class="btn btn-primary">
				<i class="bi bi-plus-square me-2"></i>
				Create New Food Type
			</a>
		</div>
	</div>
	<br />
	<br />
	<table class="table table-bordered table-striped" style="width: 100%">
		<thead>
			<tr>
				<th style="width: 80%">Food Type Name</th>
				<th style="width: 20%"></th>
			</tr>
		</thead>
		<tbody>
			@if (Model.FoodTypes.Count() > 0)
			{
				foreach (var foodType in Model.FoodTypes)
				{
					<tr>
						<td>@foodType.Name</td>
						<td class="text-center">
							<div class="w-75 btn-group" role="group">
								<a asp-page="Edit" asp-route-id="@foodType.Id" class="btn btn-primary mx-2">
									<i class="bi bi-pencil-square"></i>
								</a>
								<a asp-page="Delete" asp-route-id="@foodType.Id" class="btn btn-danger mx-2">
									<i class="bi bi-trash-fill"></i>
								</a>
							</div>
						</td>
					</tr>
				}
			}
			else
			{
				<tr>
					<td colspan="2" class="fst-italic text-center">&lt;No food types to display&gt;</td>
				</tr>
			}
		</tbody>
	</table>
</div>
